{
    "layer": "bottom",
    "position": "top",
	"height": 20,
	"modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": [ "network#wifi", "network#wg0", "network#tun0", "battery", "clock", "tray" ],
    "sway/workspaces": {
        "disable-scroll": false,
        "all-outputs": false,
        "numeric-first": true,
        "format": "{name}"
    },
    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>"
    },
    "tray": {
        // "icon-size": 21,
        "spacing": 10
    },
    "clock": {
        "format": "{:%Y-%m-%d  %H:%M}"
    },
    "temperature": {
        // "thermal-zone": 2,
        // "hwmon-path": "/sys/class/hwmon/hwmon2/temp1_input",
        "critical-threshold": 80,
        // "format-critical": "{temperatureC}°C ",
        "format": "{temperatureC}°C "
    },
    "battery": {
        "states": {
            "good": 95,
            "warning": 30,
            "critical": 5
        },
        "format": "bat: {capacity}%",
        // "format": "{capacity}% {icon}",
        // "format-good": "", // An empty format will hide the module
        // "format-full": "",
        "format-icons": ["", "", "", "", ""]
    },
    "network#wifi": {
        "interface": "wlp0s20f3",
        "format-wifi": " wifi:{ipaddr}/ ({signalStrength}%) ",
        "format-ethernet": "{ifname}:{ipaddr}/{cidr}",
        "format-disconnected": "Disconnected"
    },
    "network#wg0": {
        "interface": "wg0",
        "format-ethernet": "{ifname}:{ipaddr}/{cidr}",
        "format-disconnected": "Disconnected"
    },
    "network#tun0": {
        "interface": "tun0",
        "format-ethernet": "{ifname}:{ipaddr}/{cidr}",
        "format-disconnected": "Disconnected"
    }
}
